global proc vstLock(string $stuff[])
{
	$DLAM = "OFF";
	if (!`objExists $DLAM`)	
	{
		group -empty -n $DLAM;
		hide $DLAM;
		
		createDisplayLayer -name "OFF" -number 1 -empty;
		setAttr OFF.displayType 2;
		editDisplayLayerMembers OFF $DLAM;
	}	
	editDisplayLayerMembers OFF $stuff;
}
