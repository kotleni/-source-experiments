global proc brLock (string $stuff[])
{
	$DLAM = "OFF";
	if (!`objExists $DLAM`)	
	{
		//group -empty -n $DLAM;
		//hide $DLAM;
		
		createDisplayLayer -name "OFF" -number 1 -empty;
		layerEditorLayerButtonTypeChange OFF;
		layerEditorLayerButtonTypeChange OFF;
		select -r $stuff ;
		layerEditorAddObjects OFF;
	}	
	else
	{
		select -r $stuff ;
		layerEditorAddObjects OFF;
	}
}
